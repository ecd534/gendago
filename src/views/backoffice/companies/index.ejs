<!DOCTYPE html>
<html lang="pt-BR">
<head>
	<meta charset="UTF-8">
	<meta name="viewport" content="width=device-width, initial-scale=1.0">
	<title><%= title %> | Gendago</title>
	<link rel="preconnect" href="https://cdn.jsdelivr.net">
	<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.7/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-LN+7fdVzj6u52u30Kp6M/trliBMCMKTyK833zpbD+pXdCLuTusPj697FH4R/5mcr" crossorigin="anonymous">
	<link rel="stylesheet" href="/styles/admin.css">
</head>
<body>
	<div class="container-fluid backoffice-shell">
		<div class="row min-vh-100">
			<%- include('../partials/sidebar', { currentAdmin, page }) %>

			<main class="col-12 col-lg-9 col-xl-10 content-wrap">
				<section class="glass-card p-4 p-xl-5 mb-4">
					<div class="d-flex flex-column flex-md-row justify-content-between align-items-md-center gap-3">
						<div>
							<p class="text-uppercase small fw-semibold text-secondary mb-2">Cadastro</p>
							<h1 class="display-6 fw-semibold mb-2">Empresas</h1>
							<p class="text-secondary mb-0">Modulo exclusivo do master para listar, cadastrar e controlar rapidamente o status das empresas.</p>
						</div>
						<a href="/admin/empresas/nova" class="btn btn-dark rounded-pill px-4">Nova empresa</a>
					</div>
				</section>

				<% if (flashMessage) { %>
					<div class="alert alert-<%= flashMessage.type %> border-0 rounded-4 mb-4" role="alert"><%= flashMessage.text %></div>
				<% } %>

				<section class="glass-card p-4">
					<div class="table-responsive">
						<table class="table align-middle admin-table mb-0">
							<thead>
								<tr>
									<th>Empresa</th>
									<th>Contato</th>
									<th>Slug</th>
									<th>Status</th>
									<th class="text-end">Acoes</th>
								</tr>
							</thead>
							<tbody>
								<% companies.forEach((company) => { %>
									<tr>
										<td data-label="Empresa">
											<div class="fw-semibold"><%= company.nome %></div>
											<div class="small text-secondary"><%= company.endereco || 'Endereco nao informado' %></div>
											<div class="small text-secondary"><%= company.id %></div>
										</td>
										<td data-label="Contato">
											<div><%= company.telefone || 'Telefone nao informado' %></div>
											<div class="small text-secondary"><%= company.email || 'E-mail nao informado' %></div>
										</td>
										<td data-label="Slug"><%= company.slug || 'Sem slug' %></td>
										<td data-label="Status">
											<span class="badge rounded-pill <%= company.status === 'Ativa' ? 'badge-status-active' : 'badge-status-inactive' %>">
												<%= company.status %>
											</span>
										</td>
										<td data-label="Acoes">
											<div class="d-flex justify-content-end gap-2 flex-wrap">
												<a href="/admin/empresas/<%= company.id %>/editar" class="btn btn-sm btn-outline-dark rounded-pill">Editar</a>
												<form action="/admin/empresas/<%= company.id %>/status" method="post">
													<input type="hidden" name="status" value="<%= company.status === 'Ativa' ? 'Inativa' : 'Ativa' %>">
													<button type="submit" class="btn btn-sm rounded-pill <%= company.status === 'Ativa' ? 'btn-outline-danger' : 'btn-outline-success' %>"><%= company.status === 'Ativa' ? 'Inativar' : 'Ativar' %></button>
												</form>
											</div>
										</td>
									</tr>
								<% }) %>
							</tbody>
						</table>
					</div>
				</section>
			</main>
		</div>
	</div>
	<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.7/dist/js/bootstrap.bundle.min.js" integrity="sha384-ndDqU0Gzau9qJ1lfW4pNLlhNTkCfHzAVBReH9diLvGRem5+R9g2FzA8ZGN954O5Q" crossorigin="anonymous"></script>
</body>
</html>